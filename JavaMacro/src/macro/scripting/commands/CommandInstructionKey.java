package macro.scripting.commands;

import macro.instruction.Instruction;
import macro.scripting.Command;

public class CommandInstructionKey implements Command {

        @Override
        public void execute(Instruction instruction) {
        }
}
